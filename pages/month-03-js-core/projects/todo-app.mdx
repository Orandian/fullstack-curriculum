---
title: Project — Todo App (Pure JS)
---

import { Callout, FileTree } from 'nextra/components'

# Project — Todo App (Pure JS)

**Goal:** Build a fully functional todo app using only HTML, CSS, and JavaScript — no frameworks.

## Requirements

- [ ] Add todos with a text input
- [ ] Mark todos as complete (toggle)
- [ ] Delete individual todos
- [ ] Filter: All / Active / Completed
- [ ] Show count of remaining todos
- [ ] Clear all completed todos
- [ ] Persist todos in localStorage
- [ ] Keyboard: press Enter to add

## File Structure

<FileTree>
  <FileTree.Folder name="todo-app" defaultOpen>
    <FileTree.File name="index.html" />
    <FileTree.File name="style.css" />
    <FileTree.File name="script.js" />
  </FileTree.Folder>
</FileTree>

## Full Source Code

### script.js

```js filename="script.js"
// =====================
// STATE
// =====================
let todos = JSON.parse(localStorage.getItem('todos')) || [];
let filter = 'all'; // 'all' | 'active' | 'completed'

// =====================
// DOM REFERENCES
// =====================
const form      = document.querySelector('#todo-form');
const input     = document.querySelector('#todo-input');
const list      = document.querySelector('#todo-list');
const countEl   = document.querySelector('#todo-count');
const clearBtn  = document.querySelector('#clear-completed');
const filterBtns = document.querySelectorAll('.filter-btn');

// =====================
// SAVE TO LOCALSTORAGE
// =====================
function save() {
  localStorage.setItem('todos', JSON.stringify(todos));
}

// =====================
// RENDER
// =====================
function getFilteredTodos() {
  if (filter === 'active')    return todos.filter(t => !t.done);
  if (filter === 'completed') return todos.filter(t => t.done);
  return todos;
}

function render() {
  const filtered = getFilteredTodos();

  list.innerHTML = filtered.map(todo => `
    <li class="todo-item ${todo.done ? 'done' : ''}" data-id="${todo.id}">
      <button class="toggle-btn" aria-label="Toggle complete">
        ${todo.done ? '✅' : '⬜'}
      </button>
      <span class="todo-text">${escapeHtml(todo.text)}</span>
      <button class="delete-btn" aria-label="Delete todo">✕</button>
    </li>
  `).join('');

  // Update count
  const activeCount = todos.filter(t => !t.done).length;
  countEl.textContent = `${activeCount} item${activeCount !== 1 ? 's' : ''} left`;

  // Update filter buttons
  filterBtns.forEach(btn => {
    btn.classList.toggle('active', btn.dataset.filter === filter);
  });
}

// Prevent XSS
function escapeHtml(str) {
  const div = document.createElement('div');
  div.textContent = str;
  return div.innerHTML;
}

// =====================
// ACTIONS
// =====================
function addTodo(text) {
  const trimmed = text.trim();
  if (!trimmed) return;
  todos.push({ id: Date.now(), text: trimmed, done: false });
  save();
  render();
}

function toggleTodo(id) {
  todos = todos.map(t => t.id === id ? { ...t, done: !t.done } : t);
  save();
  render();
}

function deleteTodo(id) {
  todos = todos.filter(t => t.id !== id);
  save();
  render();
}

function clearCompleted() {
  todos = todos.filter(t => !t.done);
  save();
  render();
}

// =====================
// EVENT LISTENERS
// =====================
form.addEventListener('submit', (e) => {
  e.preventDefault();
  addTodo(input.value);
  input.value = '';
});

// Event delegation — one listener for the whole list
list.addEventListener('click', (e) => {
  const item = e.target.closest('.todo-item');
  if (!item) return;
  const id = Number(item.dataset.id);

  if (e.target.classList.contains('toggle-btn')) toggleTodo(id);
  if (e.target.classList.contains('delete-btn'))  deleteTodo(id);
});

filterBtns.forEach(btn => {
  btn.addEventListener('click', () => {
    filter = btn.dataset.filter;
    render();
  });
});

clearBtn.addEventListener('click', clearCompleted);

// =====================
// INIT
// =====================
render();
```

### index.html

```html filename="index.html"
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Todo App</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="app">
    <h1>📝 Todo App</h1>

    <form id="todo-form" class="add-form">
      <input
        type="text"
        id="todo-input"
        placeholder="What needs to be done?"
        autocomplete="off"
        autofocus
      />
      <button type="submit">Add</button>
    </form>

    <div class="todo-card">
      <ul id="todo-list"></ul>

      <div class="footer">
        <span id="todo-count">0 items left</span>
        <div class="filters">
          <button class="filter-btn active" data-filter="all">All</button>
          <button class="filter-btn" data-filter="active">Active</button>
          <button class="filter-btn" data-filter="completed">Completed</button>
        </div>
        <button id="clear-completed">Clear Completed</button>
      </div>
    </div>
  </div>
  <script src="script.js"></script>
</body>
</html>
```

### style.css

```css filename="style.css"
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: system-ui, sans-serif;
  background: #f3f4f6;
  min-height: 100vh;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 40px 16px;
}

.app { width: 100%; max-width: 480px; }

h1 { font-size: 2rem; text-align: center; margin-bottom: 24px; color: #1f2937; }

.add-form {
  display: flex;
  gap: 8px;
  margin-bottom: 16px;
}
.add-form input {
  flex: 1;
  padding: 12px 16px;
  border: 2px solid #e5e7eb;
  border-radius: 8px;
  font-size: 1rem;
  outline: none;
}
.add-form input:focus { border-color: #1d4ed8; }
.add-form button {
  padding: 12px 20px;
  background: #1d4ed8;
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
}

.todo-card {
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  overflow: hidden;
}

#todo-list { list-style: none; }

.todo-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 16px;
  border-bottom: 1px solid #f3f4f6;
}
.todo-item.done .todo-text {
  text-decoration: line-through;
  color: #9ca3af;
}

.toggle-btn, .delete-btn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.1rem;
  padding: 2px;
  line-height: 1;
}
.delete-btn { margin-left: auto; color: #d1d5db; }
.delete-btn:hover { color: #ef4444; }

.todo-text { flex: 1; }

.footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 8px;
  padding: 12px 16px;
  font-size: 0.85rem;
  color: #6b7280;
  border-top: 1px solid #f3f4f6;
}

.filters { display: flex; gap: 4px; }
.filter-btn {
  background: none;
  border: 1px solid transparent;
  border-radius: 4px;
  padding: 2px 8px;
  cursor: pointer;
  color: #6b7280;
  font-size: 0.85rem;
}
.filter-btn.active { border-color: #1d4ed8; color: #1d4ed8; }
.filter-btn:hover { border-color: #d1d5db; }

#clear-completed { background: none; border: none; cursor: pointer; color: #6b7280; font-size: 0.85rem; }
#clear-completed:hover { color: #ef4444; }
```

<Callout type="info">
  **Key patterns used:**
  - **Event delegation** — one listener on the list, not one per item
  - **State → render** — always update state first, then re-render
  - **localStorage** — persist data between page reloads
  - **XSS prevention** — `escapeHtml()` prevents injection attacks
</Callout>
