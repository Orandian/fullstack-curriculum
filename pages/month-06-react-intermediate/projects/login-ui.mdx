---
title: Project 2 — Login UI (Frontend Only)
---

import { Callout } from 'nextra/components'

# Project 2 — Login UI (Frontend Only)

**Goal:** Build a complete auth flow — Login, Register, and a protected Dashboard — using React Router, useState, and Context (no real backend).

## Pages

- `/login` — email + password form
- `/register` — name, email, password form
- `/dashboard` — protected page (redirects to /login if not "logged in")

## Core Implementation

### AuthContext.jsx

```jsx filename="src/context/AuthContext.jsx"
import { createContext, useContext, useState } from 'react';
import { useNavigate } from 'react-router-dom';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(() => {
    const saved = localStorage.getItem('user');
    return saved ? JSON.parse(saved) : null;
  });
  const navigate = useNavigate();

  function login(email, password) {
    // Mock auth — in Month 10 you'll connect to a real API
    if (email && password.length >= 8) {
      const mockUser = { id: 1, name: email.split('@')[0], email };
      setUser(mockUser);
      localStorage.setItem('user', JSON.stringify(mockUser));
      navigate('/dashboard');
      return true;
    }
    return false;
  }

  function register(name, email, password) {
    const mockUser = { id: Date.now(), name, email };
    setUser(mockUser);
    localStorage.setItem('user', JSON.stringify(mockUser));
    navigate('/dashboard');
  }

  function logout() {
    setUser(null);
    localStorage.removeItem('user');
    navigate('/login');
  }

  return (
    <AuthContext.Provider value={{ user, login, register, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export const useAuth = () => useContext(AuthContext);
```

### pages/LoginPage.jsx

```jsx filename="src/pages/LoginPage.jsx"
import { useState } from 'react';
import { Link } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';

function LoginPage() {
  const { login } = useAuth();
  const [form, setForm] = useState({ email: '', password: '' });
  const [error, setError] = useState('');

  function handleChange(e) {
    setForm(prev => ({ ...prev, [e.target.name]: e.target.value }));
  }

  async function handleSubmit(e) {
    e.preventDefault();
    setError('');
    const ok = login(form.email, form.password);
    if (!ok) setError('Invalid credentials. Password must be 8+ characters.');
  }

  return (
    <div className="auth-page">
      <div className="auth-card">
        <h1>Welcome back</h1>
        <p>Sign in to your account</p>

        <form onSubmit={handleSubmit}>
          {error && <div className="alert alert-error">{error}</div>}
          <div className="field">
            <label>Email</label>
            <input type="email" name="email" value={form.email} onChange={handleChange} required />
          </div>
          <div className="field">
            <label>Password</label>
            <input type="password" name="password" value={form.password} onChange={handleChange} required />
          </div>
          <button type="submit" className="btn-primary">Sign In</button>
        </form>

        <p className="auth-footer">
          Don't have an account? <Link to="/register">Register</Link>
        </p>
      </div>
    </div>
  );
}

export default LoginPage;
```

### pages/DashboardPage.jsx

```jsx filename="src/pages/DashboardPage.jsx"
import { useAuth } from '../context/AuthContext';

function DashboardPage() {
  const { user, logout } = useAuth();
  return (
    <div className="dashboard">
      <header className="dash-header">
        <h1>Dashboard</h1>
        <div className="user-info">
          <span>👋 Hello, {user.name}!</span>
          <button onClick={logout}>Logout</button>
        </div>
      </header>
      <main>
        <div className="stats-grid">
          <div className="stat-card"><h3>Projects</h3><p className="stat-num">3</p></div>
          <div className="stat-card"><h3>Completed</h3><p className="stat-num">12</p></div>
          <div className="stat-card"><h3>Streak</h3><p className="stat-num">7 days</p></div>
        </div>
      </main>
    </div>
  );
}

export default DashboardPage;
```

<Callout type="info">
  In Month 10, you'll replace the mock `login()` function with a real API call to your Node.js backend. The UI stays exactly the same.
</Callout>
