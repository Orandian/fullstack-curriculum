---
title: レッスン4 — テスト
---

import { Callout } from 'nextra/components'

# テスト（Vitest + React Testing Library）

テストはバグを早期に発見し、リファクタリングを安全にします。

## セットアップ

```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

## コンポーネントのテスト

```tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import Counter from './Counter'

describe('Counter', () => {
  it('初期値0で表示される', () => {
    render(<Counter />)
    expect(screen.getByText('カウント: 0')).toBeInTheDocument()
  })

  it('+1ボタンをクリックするとカウントが増える', () => {
    render(<Counter />)
    fireEvent.click(screen.getByText('+1'))
    expect(screen.getByText('カウント: 1')).toBeInTheDocument()
  })
})
```

<Callout type="info">
  テストは実装の詳細ではなく、**ユーザーの視点**でテストします。「+1ボタンをクリックしたら数字が増える」という動作をテストします。
</Callout>

## 演習

1. `Button`コンポーネントのテストを書く
2. フォームのバリデーションテストを書く
3. APIモックを使ったデータ表示のテストを書く
